// Этот файл service worker'а необходим для того,
// чтобы браузер распознал сайт как устанавливаемое приложение (PWA).

// Пустой обработчик события fetch.
// Он позволяет сайту проходить аудит PWA, но не меняет поведение загрузки.
self.addEventListener('fetch', (event) => {
  // Ничего не делаем, просто позволяем запросу пройти как обычно.
});
